version: "3"
services:
  postgresql:
    image: "postgres"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=pizza_app
    ports:
      - "5432:5432"
